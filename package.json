{
  "name": "aggregation-test",
  "description": "test package",
  "version": "0.0.5",
  "dependencies": {
    "ajv": "^8.6.2",
    "ajv-formats": "^2.1.1",
    "ajv-keywords": "^5.0.0",
    "pino": "^6.13.2",
    "prettier": "^2.4.0",
    "redis": "^3.1.2",
    "test-package": "^0.0.1",
    "tslint": "^6.1.3",
    "yup": "^0.32.9"
  },
  "devDependencies": {
    "@types/redis": "^2.8.31"
  },
  "scripts": {
    "jest": "jest",
    "build": "tsc -p tsconfig.json ",
    "watch": "node_modules/typescript/bin/tsc -w --preserveWatchOutput",
    "fixFormat": "prettier --write .",
    "test": "prettier --check . && tslint -c tslint.json 'src/**/*.ts' && jest && npm audit"
  }
}
